{% extends '::header.html.twig' %}

{% block content %}
    <div id="content">
        <div class="wrapper">
            <h1>Editer une course</h1>

    {{ form(edit_form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_course') }}">
                Retour à la liste
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
        </div>
    </div>
{% endblock %}
